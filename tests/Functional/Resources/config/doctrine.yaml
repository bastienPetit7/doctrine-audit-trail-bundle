doctrine:
    dbal:
        default_connection: default
        connections:
            default:
                url: 'sqlite:///:memory:'
            audit:
                url: 'sqlite:///:memory:'
    orm:
        default_entity_manager: default
        entity_managers:
            default:
                connection: default
                auto_mapping: false
            audit:
                connection: audit
